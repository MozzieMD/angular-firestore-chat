import { TimeAgoCustomClock } from './time-ago-custom-clock';

describe('TimeAgoCustomClock', () => {
  it('should create an instance', () => {
    expect(new TimeAgoCustomClock()).toBeTruthy();
  });
});
